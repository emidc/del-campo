# Evidencia de ejecución — T-0012

**Procedencia: escrita durante la ejecución**, 2026-09-21, en la máquina de desarrollo
(macOS, Apple Silicon; Node 24; pnpm 11.19.0; PostgreSQL 17.11 por Homebrew, la misma
versión mayor que `.postgres-version`). No es un retrofit. → R-09b

| Contexto | Valor |
| --- | --- |
| SHA de main al empezar | `d3bd14d70d45662236fdea908d65a303f56dbaff` |
| Rama | `task/T-0012-schema-con-invariantes-como-constraints` |
| AgentRun | El `runId` de esta sesión queda registrado por el hook `SessionStart`/`SessionEnd` en `ops/runs/2026-09-21.jsonl`. → R-12 |

**Procedencia de la corrección post revisión ciega (BR-010).** La sección
`## Corrección tras la revisión ciega` de más abajo se escribió en una sesión distinta
de la que generó el resto de este documento. Su procedencia, tal como quedó registrada
y sin reconstruir lo que no lo esté:

| Contexto | Valor |
| --- | --- |
| SHA de HEAD verificado al cerrar la corrección | `67fc5de269f7fd0510ce9a20bec7a335557207f1` |
| Rama | `task/T-0012-schema-con-invariantes-como-constraints` (misma) |
| `runId` de esta sesión | `r_8729729c7f39980b6452` (`RUN_STARTED`, `ops/runs/2026-09-21.jsonl`, `taskId: T-0012`, `providerSessionId: 7c0f7ed3-ac7a-45ee-9ab3-535d5a0c819b`) |
| `runId` de la sesión que generó el reporte de revisión ciega | No quedó registrado en `ops/runs/2026-09-21.jsonl` con `taskId: T-0012` separado del run de implementación; `REVIEWS/T-0012/blind-review.md` no incluye su propio `runId`. Se declara faltante en vez de reconstruirlo. |

## Preflight

Antes del PASO 0: working tree limpio salvo el log de AgentRun (append-only, ajeno a
esta tarea), rama de partida `main` al día con `origin/main`, `T-0012` presente en el
merge-base (confirmado en `TASKS/T-0012-schema-con-invariantes-como-constraints.md`,
histórico visible desde `T-0015`). Baseline de `pnpm check` corrido **antes** de tocar
nada de T-0012: verde, 55/55, exit 0.

## Secuencia de estado

1. `main` → rama `task/T-0012-schema-con-invariantes-como-constraints`.
2. Commit `796f14f`: `DRAFT` → `READY`, con la respuesta del owner a Q-12 registrada en
   `## Notes`. Solo tocó `## Notes` y el front-matter `status`; `## Why`, `## Outcome`,
   `## Non-scope` y `## Verification` no se modificaron (confirmado con
   `git diff ... | grep -E "^[+-]## "`, sin salida).
3. Commit `1585a82`: `READY` → `ACTIVE`, aislado, antes de escribir SQL.
4. Implementación (esta evidencia).
5. Esta verificación.

## Comandos y salida literal

### Baseline — `pnpm check` antes de cualquier cambio de T-0012

Exit `0`. Salida completa (55 tests, todos verdes) omitida acá por longitud; el mismo
comando se repite íntegro más abajo, después de la implementación, con la salida
completa incluida.

### Migración sobre base limpia — `pnpm db:reset`

```text
$ node packages/db/src/cli.ts reset
✓ base eliminada: delcampo_dev
✓ base creada: delcampo_dev
✓ 0001_vs01_core_schema.sql
```

### Reconstructibilidad — `pnpm db:migrate` inmediatamente después, sin cambios

```text
$ node packages/db/src/cli.ts migrate
· 1 migración(es), ninguna pendiente
```

**Sobre "hacia adelante y hacia atrás" de `## Verification`.** El runner de
`packages/db/src/cli.ts` (T-0010) sólo aplica migraciones hacia adelante; no existe
`down`, y no se introduce un contrato general de down migrations para esta tarea sola
— eso sería una decisión de arquitectura no pedida por el `## Outcome`. Lo que se
verifica en su lugar es **reconstruibilidad del entorno local**: `pnpm db:reset`
destruye la base y la recrea aplicando `0001_vs01_core_schema.sql` desde cero sin
error, y una segunda corrida de `pnpm db:migrate` confirma que el ledger queda
consistente (cero pendientes). Es la interpretación que el propio `packages/db/migrations/README.md`
de T-0010 anticipa, y no equivale a poder revertir una migración individual ya
aplicada sobre datos existentes — eso no se necesitó ni se intentó.

### Tablas creadas

```text
                      List of relations
 Schema |          Name           | Type  |      Owner
--------+-------------------------+-------+------------------
 public | app_user                | table | emilianodelcampo
 public | contact_point           | table | emilianodelcampo
 public | document_link           | table | emilianodelcampo
 public | endorsement             | table | emilianodelcampo
 public | external_reference      | table | emilianodelcampo
 public | insurer                 | table | emilianodelcampo
 public | insurer_alias           | table | emilianodelcampo
 public | organization_membership | table | emilianodelcampo
 public | organization_profile    | table | emilianodelcampo
 public | party                   | table | emilianodelcampo
 public | party_role              | table | emilianodelcampo
 public | person_profile          | table | emilianodelcampo
 public | policy                  | table | emilianodelcampo
 public | policy_version          | table | emilianodelcampo
 public | schema_migrations       | table | emilianodelcampo
(15 rows)
```

### Observación externa — rechazo real de Postgres, fuera del framework de tests

Por fuera de `node --test`, directamente con `psql`, en una transacción revertida
(los datos son sintéticos, R-19: ningún dato de cliente real entra a esta tarea):

```text
$ psql postgres://localhost:5432/delcampo_dev
BEGIN
INSERT 0 1
INSERT 0 1
INSERT 0 1
INSERT 0 1
INSERT 0 1
INSERT 0 1
INSERT 0 1
ERROR:  conflicting key value violates exclusion constraint "policy_version_policy_id_daterange_excl"
DETAIL:  Key (policy_id, daterange(effective_from, COALESCE(effective_to, 'infinity'::date)))=(3b640134-c218-4dbb-99d1-1a6eb85f746d, [2026-03-01,2026-09-01)) conflicts with existing key (policy_id, daterange(effective_from, COALESCE(effective_to, 'infinity'::date)))=(3b640134-c218-4dbb-99d1-1a6eb85f746d, [2026-01-01,2026-06-01)).
ROLLBACK
```

Esta es la observación externa que `## Why` de la tarea pide: un intento real de
insertar dos `PolicyVersion` solapadas, rechazado por Postgres, no por `psql` ni por
el runner de tests interpretando algo.

### Tests de integración — `node --test packages/db/src/schema.integration.test.ts`

```text
▶ positivos — el schema admite el flujo válido de VS01
  ✔ Party PERSON, PartyRole PROSPECT, ContactPoint, y una Policy con su PolicyVersion abierta (29.180208ms)
  ✔ coverage_data queda NULL sin que ningún DEFAULT lo complete (2.192917ms)
✔ positivos — el schema admite el flujo válido de VS01 (31.860458ms)
▶ negativos — la base rechaza, no la aplicación
  ✔ INV-PV-003: dos PolicyVersion con intervalos solapados (3.358375ms)
  ✔ INV-PV-004: dos PolicyVersion abiertas para la misma Policy (1.814667ms)
  ✔ INV-006: dos PartyRole PROSPECT activos para la misma Party (1.470125ms)
  ✔ INV-003: un merge que formaría un ciclo (1.539291ms)
  ✔ INV-020: dos Policy con el mismo par (insurerId, policyNumber) (0.991583ms)
  ✔ INV-023: una PolicyVersion sin holder_party_id (1.427084ms)
  ✔ INV-022: modificar source_value de un ExternalReference ya insertado (1.002375ms)
✔ negativos — la base rechaza, no la aplicación (11.885584ms)
▶ causalidad — cada test negativo mide el mecanismo que dice medir
  ✔ INV-PV-003/004: retirando el EXCLUDE de policy_version, el solapamiento se acepta; restaurado, se vuelve a rechazar (5.951125ms)
  ✔ INV-PV-004: el índice único parcial NO es la causa real mientras el EXCLUDE exista (hallazgo documentado en D-0051) (2.508875ms)
  ✔ INV-006: retirando el EXCLUDE de party_role, dos roles activos se aceptan; restaurado, se vuelve a rechazar (2.990417ms)
  ✔ INV-003: retirando el trigger de ciclos, un merge cíclico se acepta; restaurado, se vuelve a rechazar (3.068334ms)
  ✔ INV-022: retirando el trigger de inmutabilidad, source_value se puede sobrescribir; restaurado, se vuelve a rechazar (1.764958ms)
✔ causalidad — cada test negativo mide el mecanismo que dice medir (16.4315ms)
ℹ tests 14
ℹ suites 3
ℹ pass 14
ℹ fail 0
```

**Sobre la prueba de causalidad de INV-PV-004.** El primer test de causalidad de
`INV-PV-003/004` retira el `EXCLUDE` y demuestra que, sin él, tanto el solapamiento
cerrado como el de dos versiones abiertas se aceptan (ver también el segundo test,
específico de esa comparación). El segundo test retira **solo** el índice único
parcial `policy_version_one_open_per_policy`, dejando el `EXCLUDE` intacto, e inserta
dos versiones abiertas: el resultado es que **siguen rechazadas**. Esto prueba, contra
la base real y no por argumento, que el índice parcial no es la causa del rechazo
mientras el `EXCLUDE` exista — está documentado en el ADR D-0051 y no se reporta como
una prueba de causalidad independiente que no sería real.

### `pnpm check` completo, con typecheck/lint/test cableados — salida íntegra

```text
$ node scripts/check-docs.mjs && node scripts/check-agent-run.mjs && node --test scripts/tests/*.test.mjs && pnpm typecheck && pnpm lint && pnpm test

✓ 51 decisiones, 19 ADRs, 18 tareas, 0 aviso(s)
✓ AgentRun: 10 eventos; lifecycle, concurrencia y fallos verificados
✔ el repositorio versionado satisface R-09b tal como está (71.697167ms)
✔ una tarea DONE sin archivo de evidencia falla nombrando el archivo que falta (70.294291ms)
✔ una tarea DONE nueva pasa en cuanto su evidencia existe con las tres secciones (70.756083ms)
✔ un archivo de evidencia incompleto falla nombrando la sección ausente (71.083083ms)
✔ la regla no alcanza a una tarea DROPPED (69.927375ms)
✔ el checker funciona cuando SLICES todavía no existe (72.691208ms)
✔ admite referencias existentes en slices y subdirectorios (68.424ms)
✔ rechaza una referencia inexistente con el path del slice (69.997125ms)
✔ no omite referencias inexistentes dentro de subdirectorios (71.375958ms)
✔ un slice no puede copiar estados del índice de decisiones (71.358417ms)
✔ una rama task/T-xxxx-... es válida y expone el task id (0.73175ms)
✔ un prefijo exento (docs/) no requiere task id (0.080541ms)
✔ un prefijo exento (chore/) no requiere task id (0.043875ms)
✔ una rama con un prefijo no declarado (test/) falla la regla (a), no se cuela como exenta (0.084625ms)
✔ una rama sin task id y sin prefijo exento falla nombrando R-29 (0.047333ms)
✔ run() nunca deriva el nombre de rama de GITHUB_REF_NAME (161.842833ms)
✔ CHECK_BRANCH_NAME explícito tiene prioridad y es lo que el workflow debe cablear a github.head_ref (119.0965ms)
✔ un baseRef que no se pudo resolver localmente falla explícito, no pasa en silencio (134.006833ms)
✔ rama con task id cuya tarea no existe en el merge-base falla la regla (b) (127.919125ms)
✔ modificar ## Verification en la rama rompe la regla (c) (133.650125ms)
✔ modificar solo ## Notes pasa en verde: esa sección queda libre (136.123375ms)
✔ una rama docs/ sin tarea asociada pasa en verde (106.159542ms)
✔ un "## Verification" citado dentro de un bloque de código no parte la sección (0.098833ms)
✔ CRLF no rompe la extracción ni produce un falso verde o falso rojo (0.036667ms)
✔ una sección vacía se distingue de una sección ausente (0.027458ms)
✔ splitSections no confunde un header real que sigue a un bloque de código (0.049958ms)
✔ patch conserva deny rules y captura AgentRun; agrega solo PreToolUse (34.495708ms)
✔ deniega sed settings sin ejecutar ni cambiar la entrada (67.63825ms)
✔ deniega sed workflow sin ejecutar ni cambiar la entrada (63.615625ms)
✔ deniega GNU sed sin ejecutar ni cambiar la entrada (65.733083ms)
✔ deniega backup sed sin ejecutar ni cambiar la entrada (66.832917ms)
✔ deniega borrar ancestro sin ejecutar ni cambiar la entrada (66.16475ms)
✔ deniega mover control sin ejecutar ni cambiar la entrada (55.6645ms)
✔ deniega redirección sin ejecutar ni cambiar la entrada (55.831708ms)
✔ deniega comando compuesto sin ejecutar ni cambiar la entrada (53.781917ms)
✔ deniega traversal explícito sin ejecutar ni cambiar la entrada (55.101ms)
✔ deniega alias por symlink y ruta absoluta protegida (104.901167ms)
✔ pide revisión humana: python3 script.py (56.878542ms)
✔ pide revisión humana: node scripts/check-docs.mjs (54.236542ms)
✔ pide revisión humana: pnpm check (54.723083ms)
✔ pide revisión humana: git checkout other (53.865041ms)
✔ pide revisión humana: git apply input.patch (54.389292ms)
✔ pide revisión humana: bash script.sh (54.169166ms)
✔ pide revisión humana: p=target; sed -i '' 's/before/after/' "$p" (51.691833ms)
✔ pide revisión humana: sed -i '' 's/before/after/' TASKS/*.md (54.410167ms)
✔ pide revisión humana: sed -i '' 's/before/after/e' TASKS/example.md (51.626417ms)
✔ pide revisión humana: sed -i '' 's/before/after/;w output' TASKS/example.md (53.174708ms)
✔ pide revisión humana: sed -i '' -f program.sed TASKS/example.md (51.879625ms)
✔ pide revisión humana: sed -i '' 's/before/after/' $(printf target) (52.417667ms)
✔ pide revisión humana: cat TASKS/example.md && node another.mjs (53.365ms)
✔ hardlinks y cwd externo no reciben pase automático (78.939667ms)
✔ lectura simple de configuración continúa por permisos normales (49.690708ms)
✔ sed permitido sobre TASKS se ejecuta realmente solo en la copia temporal (55.051417ms)
✔ JSON inválido, herramienta equivocada y path ilegible se deniegan (135.422666ms)
✔ si el hook está instalado, coincide con la propuesta probada y está conectado (0.266208ms)
ℹ tests 55
ℹ suites 0
ℹ pass 55
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 1754.113292
$ tsc --noEmit
$ eslint .
$ node --test "packages/**/*.test.ts"
▶ parsearNombre
  ✔ acepta NNNN_slug.sql y devuelve el número que ordena (0.514416ms)
  ✔ rechaza un nombre sin número, nombrando lo que esperaba (0.2445ms)
  ✔ rechaza un número de menos de cuatro dígitos: 2 y 10 no ordenan como texto (0.048583ms)
✔ parsearNombre (1.257958ms)
▶ archivosDeMigracion
  ✔ ordena por número y no por el orden del directorio (0.130917ms)
  ✔ ignora lo que no es .sql, como el README del directorio (0.039167ms)
  ✔ rechaza dos migraciones con el mismo número (0.043625ms)
  ✔ un directorio vacío es una lista vacía, no un error (0.048292ms)
✔ archivosDeMigracion (0.370542ms)
▶ pendientes
  ✔ devuelve sólo lo no aplicado, en orden (0.146792ms)
  ✔ falla con el directorio vacío y el ledger no vacío: es el caso que más fácil se esconde (0.070916ms)
  ✔ falla si la base registra una migración que ya no está en el repositorio (0.091541ms)
✔ pendientes (0.43175ms)
▶ positivos — el schema admite el flujo válido de VS01
  ✔ Party PERSON, PartyRole PROSPECT, ContactPoint, y una Policy con su PolicyVersion abierta (39.351709ms)
  ✔ coverage_data queda NULL sin que ningún DEFAULT lo complete (2.102ms)
✔ positivos — el schema admite el flujo válido de VS01 (41.925666ms)
▶ negativos — la base rechaza, no la aplicación
  ✔ INV-PV-003: dos PolicyVersion con intervalos solapados (3.590292ms)
  ✔ INV-PV-004: dos PolicyVersion abiertas para la misma Policy (2.117334ms)
  ✔ INV-006: dos PartyRole PROSPECT activos para la misma Party (1.430791ms)
  ✔ INV-003: un merge que formaría un ciclo (2.283709ms)
  ✔ INV-020: dos Policy con el mismo par (insurerId, policyNumber) (0.944917ms)
  ✔ INV-023: una PolicyVersion sin holder_party_id (1.620083ms)
  ✔ INV-022: modificar source_value de un ExternalReference ya insertado (1.113167ms)
✔ negativos — la base rechaza, no la aplicación (13.400375ms)
▶ causalidad — cada test negativo mide el mecanismo que dice medir
  ✔ INV-PV-003/004: retirando el EXCLUDE de policy_version, el solapamiento se acepta; restaurado, se vuelve a rechazar (5.819042ms)
  ✔ INV-PV-004: el índice único parcial NO es la causa real mientras el EXCLUDE exista (hallazgo documentado en D-0051) (3.109041ms)
  ✔ INV-006: retirando el EXCLUDE de party_role, dos roles activos se aceptan; restaurado, se vuelve a rechazar (3.102125ms)
  ✔ INV-003: retirando el trigger de ciclos, un merge cíclico se acepta; restaurado, se vuelve a rechazar (4.271208ms)
  ✔ INV-022: retirando el trigger de inmutabilidad, source_value se puede sobrescribir; restaurado, se vuelve a rechazar (2.04325ms)
✔ causalidad — cada test negativo mide el mecanismo que dice medir (18.54525ms)
ℹ tests 24
ℹ suites 6
ℹ pass 24
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 153.532625
```

Exit code de todo el comando: `0`.

## Los tres canarios de T-0010, repetidos sobre `pnpm check` ya cableado

Cada uno se introdujo, se comprobó que ponía `pnpm check` en rojo por la razón
correcta, y se eliminó — ninguno quedó versionado.

### Canario 1 — error de tipo

Se agregó `const canario: number = 'no es un numero'` a `packages/db/src/index.ts`.

```text
$ tsc --noEmit
packages/db/src/index.ts(3,7): error TS2322: Type 'string' is not assignable to type 'number'.
packages/db/src/index.ts(3,7): error TS6133: 'canario' is declared but its value is never read.
```

Exit code de `pnpm check`: `2`. Revertido con `git checkout -- packages/db/src/index.ts`.

### Canario 2 — violación de límite de módulo (R-25)

Se agregó a `packages/domain/src/index.ts`:

```ts
import { readFileSync } from 'node:fs'

export const canario = (): string => readFileSync('/dev/null', 'utf8')
```

```text
$ eslint .

/Users/…/packages/domain/src/index.ts
  10:1  error  packages/domain no importa nada: sólo módulos relativos dentro del paquete (R-25)  no-restricted-syntax

✖ 1 problem (1 error, 0 warnings)
```

Exit code de `pnpm check`: `1`, y por la regla correcta —`no-restricted-syntax` de
R-25 sobre `packages/domain`, no un error de tipos que hubiera enmascarado la causa
real (el primer intento, con un import sin usar, falló antes en `tsc` por variable no
usada; se corrigió para que el error observado fuera efectivamente el de límite de
módulo). Revertido con `git checkout -- packages/domain/src/index.ts`.

### Canario 3 — test que falla

Se agregó a `packages/db/src/schema.integration.test.ts`:

```ts
describe('canario T-0012', () => {
  it('falla a propósito para comprobar que pnpm check se pone en rojo', () => {
    assert.equal(1, 2)
  })
})
```

```text
test at packages/db/src/schema.integration.test.ts:540:3
✖ falla a propósito para comprobar que pnpm check se pone en rojo (0.803167ms)
  AssertionError [ERR_ASSERTION]: Expected values to be strictly equal:

  1 !== 2

    actual: 1,
    expected: 2,
    operator: 'strictEqual',
```

Exit code de `pnpm check`: `1`. Removido a mano (no con `git checkout`, porque para
entonces el archivo ya tenía cambios legítimos de la implementación); confirmado con
`grep -c "canario T-0012"` devolviendo `0` después de quitarlo, y con la corrida final
de `pnpm check` (arriba) en verde sin rastro suyo.

## Observación externa

Además de los tests de integración: el bloque `psql` de la sección "Observación
externa" corre por fuera de `node --test`, directamente contra el motor, con datos
sintéticos en una transacción revertida. Es la comprobación en el borde del sistema
que R-09b pide — el rechazo lo hace Postgres, y `psql` sólo lo está mostrando.

## Qué NO se verificó (versión original de esta evidencia — ver corrección abajo)

**Esta sección quedó desactualizada por la corrección post revisión ciega y se
conserva sin reescribir para no perder el registro de lo que efectivamente se pensó
en su momento; `## Corrección tras la revisión ciega` (al final del documento) dice
qué de esto era falso y qué se hizo.**

- ~~**Rollback de una migración individual ya aplicada.** El runner de T-0010 no lo
  soporta y esta tarea no le agrega esa capacidad~~ — **corregido (BR-008):** el
  runner ahora soporta `down`, ver la corrección al final de este documento.
- **CI real con el servicio de Postgres.** `.github/workflows/**` está denegado para
  este agente (R-13, R-15). El diff está propuesto en `REVIEWS/T-0012/` con los
  comandos exactos para aplicarlo, pero no se ejecutó en un runner de GitHub Actions;
  hasta que una persona con permiso lo aplique, el job de CI de este PR fallará en
  `pnpm test` por falta de `DATABASE_URL`/Postgres. Es un bloqueo humano pendiente, no
  maquillado con un skip. Sigue sin verificar; no forma parte de la corrección.
- ~~**INV-001, INV-002, INV-004, INV-005, INV-007 a INV-010, INV-013 a INV-019,
  INV-PV-001, INV-PV-002, INV-PV-005, INV-PV-006, INV-021.** Tienen mecanismo
  documentado en el schema o en D-0051~~ — **falso para INV-001, INV-002, INV-004 e
  INV-PV-005 (BR-009):** no existía mecanismo alguno contra `UPDATE`/`DELETE`, y
  PostgreSQL aceptaba exactamente las escrituras que estas invariantes debían
  prohibir. Corregido con triggers nuevos; ver la corrección al final del documento.
  El resto de la lista (INV-005, INV-007..010, INV-013 sin ser PV-005, INV-014..019,
  INV-PV-001, INV-PV-002, INV-PV-006, INV-021) sigue siendo una afirmación válida de
  "mecanismo documentado sin test dedicado", sin cambios.
- **Rendimiento del trigger de INV-003 sobre cadenas de merge largas.** El mecanismo
  es O(longitud de la cadena); no se midió con datos de escala real porque no existen
  todavía (T-0012 no importa nada). Sigue sin verificar.
- ~~**Carga concurrente.** Los `EXCLUDE` y triggers se probaron con escrituras
  secuenciales dentro de una transacción; no se ejecutó ningún test de dos conexiones
  escribiendo en paralelo~~ — **corregido para INV-003 (BR-001):** ver la reproducción
  con dos sesiones reales en la corrección al final del documento. El resto de los
  `EXCLUDE`/triggers sigue sin probarse bajo escritura concurrente.
- **`pnpm decisions`** no se corrió como parte de esta verificación más que para
  confirmar que el checker de `decisions.yaml`/ADRs (dentro de `pnpm check`) sigue en
  verde con las tres decisiones nuevas; no se revisó manualmente su salida human-readable
  completa. Sigue igual.

## Estado final del working tree

Al cerrar esta evidencia: rama `task/T-0012-schema-con-invariantes-como-constraints`,
sin canarios (confirmado arriba), `pnpm check` en verde (salida íntegra arriba),
mecanismos de enforcement restaurados a su estado del schema (ninguna prueba de
causalidad dejó una constraint o trigger deshabilitado: todas corrieron dentro de
transacciones descartables). `git status --short` en el momento de escribir esto
muestra únicamente los archivos nuevos y modificados de esta tarea —migración, tests,
ADRs, `decisions.yaml`, `package.json`, `packages/db/package.json`,
`pnpm-lock.yaml`, `REVIEWS/T-0012/`— más el log de AgentRun ajeno a la tarea.

## Corrección tras la revisión ciega

Una revisión ciega independiente (`REVIEWS/T-0012/blind-review.md`, sesión sin
transcript ni resumen del implementador) auditó esta misma evidencia y el diff de
`e3905d3` contra `main`. **Verdict: STOP, 3 BLOCKER y 8 MAJOR.** El estado `DONE` de
la tarea en ese momento contradecía límites materiales que la propia evidencia
reconocía (BR-011): no se había ejecutado la migración hacia atrás, y el diff de CI
protegido por R-13 no estaba aplicado. La tarea se pasó a `ACTIVE` como PASO 0 de esta
corrección.

Resumen de los 12 findings y qué se hizo con cada uno:

| Finding | Severidad | Qué encontró | Qué se hizo |
| --- | --- | --- | --- |
| BR-001 | BLOCKER | INV-003 admitía ciclos de merge bajo concurrencia: un trigger recursivo no ve las filas no commiteadas de otra transacción. Dos merges concurrentes A→B y B→A confirmaban ambos. | `pg_advisory_xact_lock` sobre clave fija en `prevent_party_merge_cycle`, serializando las escrituras del grafo de merge. Reproducido con DOS SESIONES REALES simultáneas (no secuencial) en `schema.integration.test.ts`, antes y después del fix. |
| BR-002 | BLOCKER | INV-PV-005/INV-013 sin enforcement: una `PolicyVersion` cerrada aceptaba `UPDATE`/`DELETE`. | Trigger `policy_version_closed_immutable`, distinguiendo por `OLD.effective_to`; no bloquea el `INSERT` de historia ya cerrada (carga inicial de T-0013). Test negativo + causal. |
| BR-003 | BLOCKER | Identidad y historia de Party mutables/borrables: `party.id` aceptaba `UPDATE`; una Party MERGED aceptaba `DELETE`; `party_role` aceptaba `DELETE`. Contradecía además lo que D-0051 afirmaba sobre las FKs. | Triggers `party_id_immutable`, `party_merged_no_delete`, `party_role_no_delete`. Mecanismo: triggers en la migración, no privilegios de rol (eso es de T-0013/T-0016). Test negativo + causal para cada uno. |
| BR-004 | MAJOR | `PolicyVersion.endorsement_id` podía apuntar a un Endorsement de otra Policy. | FK compuesta `(endorsement_id, policy_id)` contra `endorsement (id, policy_id)`, con `UNIQUE (id, policy_id)` nuevo en `endorsement`. Test negativo + causal. |
| BR-005 | MAJOR | `UPDATE party SET kind = ...` invalidaba en silencio los triggers de `person_profile`/`organization_profile`/`insurer`/`organization_membership`, que sólo validaban el `INSERT` del hijo. | Trigger `party_kind_immutable_with_dependents`: rechaza el cambio de `kind` si existe una fila dependiente. Test de `UPDATE` + causal. |
| BR-006 | MAJOR | `DocumentLink` sobrevivía al `DELETE` de la Policy que referenciaba: el trigger existente sólo validaba `INSERT`/`UPDATE` del link. | Trigger `policy_no_delete_with_document_links`: rechaza el `DELETE` de una Policy con `document_link` asociados (`resource_type = 'POLICY'`, el único caso de VS01). Test de borrado + causal. |
| BR-007 | MAJOR | `coverage_data` podía existir sin `coverage_source_batch_id`/`coverage_source_record_id` (linaje exigido por Q-12/D-0050). | `CHECK policy_version_coverage_lineage`: si `coverage_data` no es NULL, exige ambos identificadores; los tres NULL siguen siendo "dato ausente". Test negativo + causal. Se sacó además el índice parcial redundante de INV-PV-004 (ver BR-009). |
| BR-008 | BLOCKER (tratado como obligación de código) | No existía migración hacia atrás; `pnpm db:reset` no es una migración inversa. | Comando `down` nuevo en `packages/db/src/cli.ts` (`pnpm db:down`), que revierte la última migración aplicada usando `migrations/down/<archivo>.sql`. Down real para `0001_vs01_core_schema.sql`. Demostrado `up → down → up` sobre base local limpia con salida literal (ver el bloque de comandos más abajo). |
| BR-009 | MAJOR (documental) | Esta misma evidencia y D-0051 afirmaban cobertura de INV-001, INV-002, INV-004 e INV-PV-005 sin que existiera mecanismo real: la documentación canónica afirmaba garantías que Postgres no ofrecía. | Corregido DESPUÉS de arreglar el enforcement (BR-001..BR-008), nunca antes: la tabla de mecanismos de `DECISIONS/0051-schema-vs01-invariantes-como-constraints.md` y la sección `## Qué NO se verificó` de arriba quedaron corregidas para describir lo que realmente está enforced. |
| BR-010 | MAJOR | La procedencia de esta evidencia no tenía el SHA de rama ni ningún `runId` concreto. | Completado en el bloque de procedencia al principio del documento; lo que no quedó registrado (el `runId` de la sesión que generó el reporte de revisión) se declara faltante, no se reconstruye. |
| BR-011 | MAJOR | `DONE` contradecía límites materiales admitidos por la propia evidencia (down migration no ejecutada, CI no aplicado). | PASO 0 de esta corrección: `status` pasó de `DONE` a `ACTIVE` en un commit propio, tocando sólo frontmatter. No se vuelve a poner `DONE` en esta sesión: lo decide el owner después de que un revisor en frío, sin este historial, corra de nuevo la prueba de causalidad de cada BR. |
| BR-012 | MINOR | `decisions.yaml` con `updated: 2026-09-20` pese a tener D-0049/D-0050/D-0051 del día 21; `decisionRefs` de T-0012 no las nombraba. | `updated` a `2026-09-21`; `decisionRefs` de T-0012 ahora incluye D-0049, D-0050, D-0051. Sólo frontmatter/metadata. |

### BR-008 — up → down → up, salida literal sobre base local limpia

```text
$ pnpm db:reset
$ node packages/db/src/cli.ts reset
✓ base eliminada: delcampo_dev
✓ base creada: delcampo_dev
✓ 0001_vs01_core_schema.sql

$ pnpm db:down
$ node packages/db/src/cli.ts down
✓ down: 0001_vs01_core_schema.sql

$ psql "$DATABASE_URL" -tAc "select count(*) from information_schema.tables where table_schema='public'"
1   # sólo queda schema_migrations

$ pnpm db:migrate
$ node packages/db/src/cli.ts migrate
✓ 0001_vs01_core_schema.sql

$ psql "$DATABASE_URL" -tAc "select count(*) from information_schema.tables where table_schema='public'"
15   # las 15 tablas de VS01 vuelven a existir
```

### Verificación completa tras la corrección

```text
$ pnpm db:reset && pnpm typecheck && pnpm lint && pnpm test
```

`typecheck`: exit 0, sin salida. `lint`: exit 0, sin salida. `test`: exit 0, **43/43**
tests en verde: los 24 originales, menos 1 retirado (la causalidad del índice parcial
que BR-007/BR-009 hicieron sacar ya no tiene qué medir), más un test negativo y uno
causal por cada uno de BR-001..BR-007, más los 2 tests de concurrencia real de BR-001.

Esta corrección **no** cierra ningún finding: eso lo hace un revisor en frío, sin este
historial, corriendo de nuevo la prueba de causalidad de cada BR y escribiendo el
resultado en `REVIEWS/T-0012/blind-review.md`. Tampoco vuelve a poner `DONE`: la
decide el owner después de leer ese cierre en frío.
