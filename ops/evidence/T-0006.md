# Evidencia de ejecución — T-0006

**Procedencia: escrito durante la ejecución**, el 2026-09-20. La salida de esta sección
es la que produjeron los comandos, copiada sin editar. La tarea sigue `ACTIVE`: las
cuatro comprobaciones humanas de `## Verification` están sin marcar y este archivo se
completa al cierre.

| | |
|---|---|
| SHA de `main` | `c5ebf4027633a68b67ff9557c509ffbb38fd22f3` |
| SHA de la rama | `043a811cf635fc28660a7d51f3bcad1e42dea58c` — `task/T-0006-formato-de-evidencia-de-ejecucion` |
| AgentRuns | **Ninguno.** La sesión corre por el puente de escritorio de Cowork, no por Claude Code; el hook de captura vive en `.claude/settings.json` y no se dispara. Se declara faltante y no se simula un evento. |

## Comandos y salida literal

`pnpm` no está instalado en el entorno de esta sesión y `corepack` no pudo bajarlo. Se
ejecutó la cadena exacta que `package.json` define para `check`, con
`node v22.23.2` — por debajo del `engines: node >=24` declarado. Esa diferencia queda
registrada acá y no se disimula.

```console
$ node scripts/check-docs.mjs && node scripts/check-agent-run.mjs && node --test scripts/tests/check-docs-evidence.test.mjs
✓ 41 decisiones, 11 ADRs, 18 tareas, 0 aviso(s)
✓ AgentRun: 10 eventos; lifecycle, concurrencia y fallos verificados
TAP version 13
# Subtest: el repositorio versionado satisface R-09b tal como está
ok 1 - el repositorio versionado satisface R-09b tal como está
  ---
  duration_ms: 107.410167
  type: 'test'
  ...
# Subtest: una tarea DONE sin archivo de evidencia falla nombrando el archivo que falta
ok 2 - una tarea DONE sin archivo de evidencia falla nombrando el archivo que falta
  ---
  duration_ms: 101.402167
  type: 'test'
  ...
# Subtest: una tarea DONE nueva pasa en cuanto su evidencia existe con las tres secciones
ok 3 - una tarea DONE nueva pasa en cuanto su evidencia existe con las tres secciones
  ---
  duration_ms: 102.514958
  type: 'test'
  ...
# Subtest: un archivo de evidencia incompleto falla nombrando la sección ausente
ok 4 - un archivo de evidencia incompleto falla nombrando la sección ausente
  ---
  duration_ms: 100.787292
  type: 'test'
  ...
# Subtest: la regla no alcanza a una tarea DROPPED
ok 5 - la regla no alcanza a una tarea DROPPED
  ---
  duration_ms: 103.529834
  type: 'test'
  ...
1..5
# tests 5
# suites 0
# pass 5
# fail 0
# cancelled 0
# skipped 0
# todo 0
# duration_ms 539.444834
exit=0
```

Prueba negativa, sobre una copia temporal del contexto versionado a la que se le quitó
`ops/evidence/T-0003.md`. El repositorio no se tocó:

```console
$ cp -R <contexto versionado> "$FIX" && rm "$FIX/ops/evidence/T-0003.md"
$ cd "$FIX" && node .../scripts/check-docs.mjs
error  TASKS/T-0003-bootstrap-ci-y-proteccion-main.md: status DONE exige el archivo de evidencia ops/evidence/T-0003.md (R-09b)

✗ 1 error(es), 0 aviso(s)
exit=1
```

## Observación externa

Lo que esta tarea no podía descubrir testeándose a sí misma lo descubrió al aplicarse a
trabajo ajeno. Al retrofitear las cinco tareas `DONE` apareció que **`ops/runs/` solo
tiene el día `2026-09-08.jsonl`, y sus ocho eventos son todos de T-0001**: T-0003,
T-0004, T-0011 y T-0015 no tienen AgentRun registrado. La cláusula de R-09 «el AgentRun
quedó registrado y vinculado al PR» está marcada **ACTIVA** y hoy el repositorio no la
cumple para cuatro de sus cinco tareas cerradas. El ledger que T-0001 construyó dejó de
capturar, y ningún control lo notó hasta que alguien tuvo que escribir de dónde venía
cada cierre.

El hallazgo es del mismo tipo que motiva a T-0008 y no se resuelve acá: es una tarea
nueva, no un arreglo al paso.

Un segundo hallazgo, menor: `scripts/tests/check-docs-slices.test.mjs` entró con T-0011
y nunca se incorporó a `pnpm check`. No corre ni local ni en CI.

## Qué NO se verificó

- **`pnpm check` tal como lo corre el owner.** Sin `pnpm` y con node v22 en lugar de
  v24, lo ejecutado es equivalente comando a comando, no idéntico. La corrida en CI, con
  la versión declarada, es la que decide.
- **CI.** Todavía no hay PR, así que el check requerido de `main` no opinó.
- **Las cuatro comprobaciones humanas** de `## Verification`, incluida la lectura de si
  la evidencia de T-0001 contiene salida literal o una descripción.
- **La veracidad de los cinco archivos retrofiteados.** El checker valida que las
  secciones existan. Ninguna verificación original se reejecutó, por `## Non-scope`, de
  modo que este trabajo no acredita que T-0001, T-0003, T-0004, T-0011 y T-0015 hayan
  pasado sus comandos: acredita qué quedó registrado y qué no.
- **Que el formato sobreviva a una tarea con datos reales.** Las cinco tareas cerradas
  son documentales. R-19 se aplicará por primera vez de verdad en T-0013.
