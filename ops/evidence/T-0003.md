# Evidencia de ejecución — T-0003

**Procedencia: escrito después del hecho.** Redactado el 2026-09-20 durante T-0006, a
partir de lo que quedó registrado de la ejecución original del 2026-09-08. Ningún
comando fue reejecutado para producir este archivo.

- **Producido durante la ejecución original:** las referencias a las ejecuciones de CI y
  al PR de bootstrap, y la inspección por API de la protección de `main`, todas
  anotadas en la sección `## Evidence` de la tarea.
- **No producido durante la ejecución original:** la salida textual de los comandos
  locales y el cuerpo de las respuestas de la API. No se conservaron y no se reconstruyen.

| | |
|---|---|
| SHA de `main` | `f394b4af19250972b32b56dafae2bc9d559f0364` — primer padre del merge de bootstrap |
| SHA de la rama | `23e80c86bb9ec8c18f58d9f1b1d7091177fd5709` — `task/T-0003-bootstrap-ci-y-proteccion-main` |
| Merges | `f2d19bca128532c43ef0144a222ccb1059f1ad4d` (PR #1, bootstrap) y `5f62a3dbff3d10e0de8c0969b7b37112db8ac1c6` (PR #2, cierre con `432ec30`) |
| AgentRuns | **Ninguno.** `ops/runs/` no contiene eventos con `taskId: T-0003`; el único día con ledger es `2026-09-08.jsonl` y sus ocho eventos son de T-0001. Se declara faltante. |

## Comandos y salida literal

```bash
pnpm check
git diff --check
gh workflow view "Project OS check" --repo emidc/del-campo
gh run list --workflow "Project OS check" --repo emidc/del-campo --limit 10
gh api repos/emidc/del-campo/branches/main/protection
```

**Ninguna de estas salidas quedó registrada.** Lo que sobrevive son identificadores
externos verificables, que no son salida literal pero sí referencias comprobables por un
tercero con acceso al repositorio remoto:

```text
PR de bootstrap   https://github.com/emidc/del-campo/pull/1
run 34245754018   rojo   commit a3fe3aeaa0de22e08e663f887d31cb192c6ce9bf
run 34245878376   verde  commit 23e80c86bb9ec8c18f58d9f1b1d7091177fd5709
```

El commit `a3fe3ae` («test: prove CI rejects invalid task status») introdujo
deliberadamente el estado inválido `INVALID_FOR_CI_TEST`; `23e80c8` lo revirtió. Ambos
commits están en la historia de `main` y el par puede releerse con `git show`.

## Observación externa

La comprobación central de esta tarea es, por construcción, externa al repositorio: el
mismo check pasó de rojo a verde en GitHub Actions, sobre dos commits distintos del
mismo PR, y el PR quedó bloqueado mientras estaba en rojo. Ni el workflow ni el checker
pueden fabricar ese resultado; lo emitió el servidor que hace cumplir la protección.

La configuración de protección de `main` se inspeccionó por API el 2026-09-08 — PR
requerido, check `check` requerido en modo estricto, reglas aplicadas a
administradores, force-push y borrado deshabilitados.

## Qué NO se verificó

- **El estado actual de la protección de `main`.** Lo observado tiene fecha
  2026-09-08 y la configuración remota es mutable. Este archivo no acredita hoy.
- **La respuesta literal de la API.** Se conserva la interpretación, no el JSON.
- **Que el checker detecte la clase de error, y no ese error puntual.** La prueba
  negativa usó un `status` inválido; no se ejercitó ninguna otra regla contra CI.
- **La ejecución agéntica de esta tarea.** No hay AgentRun registrado, de modo que el
  vínculo entre el run y el diff que exige R-09 no existe para T-0003.
