# Evidencia de ejecución — T-0009

**Procedencia: escrita durante la ejecución**, 2026-09-21. No es un retrofit.

| Contexto | Valor |
| --- | --- |
| SHA de main al empezar | `182451139a2855b8f20af291876fb030f438e95d` |
| SHA de rama al escribir esto | `182451139a2855b8f20af291876fb030f438e95d` (sin commit aún; los cambios de esta tarea están en el working tree de `task/T-0009-revision-ciega-por-subagente-aislado`) |
| AgentRun de esta sesión | No se buscó su `runId` explícito para este documento: la sesión es Claude Code interactiva sobre este mismo repositorio, el ledger lo escriben los hooks `SessionStart`/`SessionEnd`. No se fabrica un id que no se confirmó línea por línea. |
| Subagente aislado (reviewer) | Lanzado como agente `general-purpose` sin fork de esta conversación — arrancó sin historial, sin ver qué skills se invocaron acá ni qué archivos leyó esta sesión. Su reporte completo está en `REVIEWS/T-0009-revision-ciega-por-subagente-aislado.md`. |
| PR revisado por el subagente | https://github.com/emidc/del-campo/pull/9 (T-0010), ya mergeado en `2bba141771bbd0835d86716d2f8b06f9f3d06885`, sobre `main` en `6cc9246a9d4e9ceb07c72276804ed385b09bc4d4` |
| Entorno | macOS Apple Silicon, Node v24, pnpm 11.19.0, PostgreSQL 17.11 (Homebrew) |

## Comandos y salida literal

### `pnpm check` — sobre esta rama, con R-33 y D-0048 ya escritos

```text
$ node scripts/check-docs.mjs && node scripts/check-agent-run.mjs && node --test scripts/tests/*.test.mjs

✓ 48 decisiones, 16 ADRs, 18 tareas, 0 aviso(s)
✓ AgentRun: 10 eventos; lifecycle, concurrencia y fallos verificados
ℹ tests 55
ℹ suites 0
ℹ pass 55
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 1752.592875
```

`pnpm check` no exige nada específico de T-0009 más allá de que `decisions.yaml` (D-0048)
y `TASKS/T-0009-...md` sigan siendo válidos según el formato general: T-0009 no agrega
ningún test propio al arnés, porque su `## Outcome` es un procedimiento de revisión y una
revisión ejercitada, no código de aplicación.

### El paquete que recibió el subagente aislado, generado antes de lanzarlo

```text
$ git show 6cc9246:TASKS/T-0009-revision-ciega-por-subagente-aislado.md   # no usado: sujeto real es T-0010
$ git show main:TASKS/T-0010-entorno-de-desarrollo-ejecutable.md > .../T-0010-contrato-desde-main.md
$ git diff 6cc9246 2bba141 > .../T-0010-diff-completo.patch
$ wc -l T-0010-contrato-desde-main.md T-0010-diff-completo.patch
     136 T-0010-contrato-desde-main.md
    2413 T-0010-diff-completo.patch
    2549 total
```

### El reporte completo del subagente aislado

Reproducido íntegro, sin edición, en `REVIEWS/T-0009-revision-ciega-por-subagente-aislado.md`,
sección "Reporte del subagente, literal". No se repite acá para no duplicar la fuente; el
checker no exige que `ops/evidence/` contenga lo mismo que `REVIEWS/` palabra por palabra,
y hacerlo hubiera invitado a que las dos copias diverjan con el tiempo.

Extracto de lo que ese reporte declara sobre el orden — que es el punto central del
`## Outcome` de esta tarea:

```text
**Orden respetado:** corrí los cinco comandos de `## Verification` sobre el estado actual
de `main` a las 09:56–09:57 (timestamps de `date` capturados antes/después de cada bloque)
y recién después, a las ~10:00, leí `ops/evidence/T-0010.md`. No abrí ese archivo antes.
```

## Observación externa

La comprobación en el borde del sistema no la da un test de este repositorio: la da que
un proceso separado, sin memoria de esta conversación, ejecutando por su cuenta contra un
Postgres real y un `tsc`/`eslint` reales, haya reproducido de forma independiente el mismo
veredicto que declaró el implementador original de T-0010 (typecheck, lint, test, check y
`db:create`/`db:reset` en verde; los tres canarios fallando cuando deben fallar) **y**,
sin que se le sugiriera buscarla, haya encontrado en `decisions.yaml` la única decisión
con relación directa a esta tarea de revisión que ni el contrato ni la evidencia de T-0010
podían nombrar (`D-0048`, porque no existía cuando T-0010 se cerró). Que un proceso aislado
llegue por su cuenta a ese id, sin que nadie se lo indicara, es la señal de que el
aislamiento fue real y no una pose: si se le hubiera pasado un extracto curado de
decisiones, encontrar `D-0048` no probaría nada.

## Qué NO se verificó

1. **No se ejerció el procedimiento sobre un PR abierto en este momento.** T-0010 ya
   estaba mergeado; la revisión fue retrospectiva, no bloqueó ningún merge real. Cómo se
   comporta el procedimiento cuando el reviewer puede afectar si un PR se mergea o no —
   con la presión de tiempo que eso implica — queda sin ejercitar.
2. **No se comparó contra una revisión hecha por el mismo agente que implementó**, que es
   el contraste que probaría que el aislamiento agrega algo. El `## Outcome` de esta
   tarea no pide ese experimento — lo pide, como pregunta abierta, `D-0043` — así que no
   se armó: T-0009 ejercita el procedimiento una vez, no lo evalúa contra un control.
3. **No se ejercitó sobre una tarea con `riskClass: HIGH` ni sobre un cambio a
   `DOMAIN.md` o a un ADR aceptado**, que es donde un `Act on` real importaría más. T-0010
   es `riskClass: LOW`; que el subagente no haya encontrado nada que actuar es coherente
   con eso y no prueba que el procedimiento detecte algo grave cuando lo hay.
4. **El subagente no tuvo acceso a CI ni a GitHub Actions**: su verificación es local, la
   misma limitación que ya reconoce `ops/evidence/T-0010.md`. R-17 sigue siendo cierta acá
   también: el veredicto lo da CI, y ni la corrida original ni esta revisión lo reemplazan.
5. **No se comprobó que el subagente aislado sea, en algún sentido medible, "mejor" o
   "peor" reviewer que un humano.** Esta tarea no lo afirma en su `## Outcome`; lo que
   demuestra es que el ciclo del arnés (contrato desde `main`, diff completo, búsqueda
   propia de contexto, verificación antes que evidencia, salida categorizada) se puede
   ejecutar de punta a punta hoy, sin nuevo proveedor y sin resolver `D-0016` ni `D-0043`.
6. **Nadie revisó esto de forma independiente. Lo verificado acá lo verificó el mismo
   agente que lo escribió**, salvo el reporte del subagente aislado, que sí es
   independiente respecto de T-0010 pero no respecto de esta propia tarea (T-0009): nadie
   revisó ciegamente la revisión ciega. Es la ironía explícita de esta tarea y no se
   resuelve acá — resolverla sería otra vuelta del mismo ciclo, no esta.
