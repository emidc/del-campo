# Evidencia de ejecución — T-0015

**Procedencia: escrito después del hecho.** Redactado el 2026-09-20 durante T-0006, a
partir de lo que quedó registrado del cierre del 2026-09-20. Ningún comando fue
reejecutado para producir este archivo.

- **Producido durante la ejecución original:** las modificaciones a `DOMAIN.md` y a
  `decisions.yaml`, las ocho comprobaciones humanas aprobadas por el owner, y la
  corrección de `DOMAIN.md` §33 detectada por la revisión independiente.
- **No producido durante la ejecución original:** la salida textual de los comandos. No
  se conservó y no se reconstruye.

| | |
|---|---|
| SHA de `main` | `34ef607` — primer padre del merge |
| SHA de la rama | `bf6decd` — `task/T-0015-alinear-dominio-con-evidencia`, tras integrar `main` |
| Merge | `2b1fdd8` — PR #5 |
| AgentRuns | **Ninguno.** `ops/runs/` no contiene eventos con `taskId: T-0015`. Se declara faltante. |

## Comandos y salida literal

```bash
pnpm check
pnpm decisions
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s scripts/tests -p 'test_*zoho*.py'
git diff --check
test -z "$(git ls-files -- data/)"
```

**Ninguna de estas salidas quedó registrada.** El cierre afirma que los cinco pasaron,
que `data/` no está versionado y que no se detectó PII en el diff.

Dos de las comprobaciones humanas son, en realidad, pruebas negativas ejecutadas contra
el checker: reintroducir un resumen de estados en un documento canónico lo hace fallar, y
un `superseded_in_part_by` cuyo `by` no aparece en el `statement` también. Se ejecutaron
agregando y quitando el defecto. Su salida tampoco se conservó, pero el control que las
produce sigue en `scripts/check-docs.mjs` y es reejecutable por un tercero.

## Observación externa

La revisión independiente encontró una contradicción real en `DOMAIN.md` §33 —
`renewedFromPolicyId = NULL` presentado como prueba de ausencia de predecesora— que las
ocho comprobaciones humanas no habían detectado. Se corrigió con autorización explícita
del owner. Un hallazgo que el propio autor del cambio no vio es la observación de borde
más fuerte que produjo esta tarea.

## Qué NO se verificó

- **Que `DOMAIN.md` esté completo.** Se alineó lo que la evidencia de T-0004 obligaba a
  tocar; el resto no se auditó.
- **Que ninguna decisión `OPEN` haya quedado resuelta por redacción.** Hay una
  comprobación humana que lo afirma, no un control automático que lo impida.
- **La tabla curada de tipo de `Endorsement` → `PolicyVersion`.** Queda abierta sobre los
  trece valores observados; es trabajo humano y precede al importador.
- **La ejecución agéntica.** Sin AgentRun registrado, no hay vínculo run–diff.
