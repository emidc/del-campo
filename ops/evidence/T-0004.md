# Evidencia de ejecución — T-0004

**Procedencia: escrito después del hecho.** Redactado el 2026-09-20 durante T-0006, a
partir de lo que quedó registrado del cierre del 2026-09-19. Ningún comando fue
reejecutado para producir este archivo.

- **Producido durante la ejecución original:** los informes `REVIEWS/T-0004-insumos-vs01.md`
  y `REVIEWS/T-0004-drive-validation.md`, versionados; los artefactos locales de curación,
  deliberadamente no versionados; y las ocho comprobaciones de cierre aprobadas por el owner.
- **No producido durante la ejecución original:** la salida textual de los comandos de
  `## Verification`. No se conservó y no se reconstruye.

| | |
|---|---|
| SHA de `main` | `5de713a61cf4cce72c770a0be6fc0c2946f2748a` — primer padre del merge |
| SHA de la rama | `2506fb7564b5dd38cd2fd8b5f8af69810df63e62` — `task/T-0004-integracion` |
| Merge | `34ef607` — PR #4 |
| AgentRuns | **Ninguno.** `ops/runs/` no contiene eventos con `taskId: T-0004`. Se declara faltante. |

## Comandos y salida literal

```bash
pnpm check
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s scripts/tests -p 'test_*zoho*.py'
git diff --check
git check-ignore data/zoho-export-2026-09-16/original-zips/
git check-ignore REVIEWS/T-0004-catalog-reconciliation.md
test -z "$(git ls-files -- data/)"
```

**Ninguna de estas salidas quedó registrada.** La tarea afirma que `pnpm check` y los
diez tests de Zoho pasaron, y que los artefactos privados quedaron ignorados; es una
afirmación, no una transcripción.

La consecuencia de esos `check-ignore` sí es re-observable hoy sin reejecutar la
verificación, porque es un hecho del árbol versionado y no una salida: `data/` no tiene
ningún archivo bajo control de versiones y `REVIEWS/T-0004-catalog-reconciliation.md`
está excluido por `.gitignore`. Comprobado el 2026-09-20 leyendo `.gitignore` y el
índice de git.

## Observación externa

La observación de borde de esta tarea no es un comando: es la muestra de 24 entidades
verificada contra Drive bajo una identidad real, con 9 carpetas accesibles y 15 no
accesibles. Ese resultado vino del sistema externo, contradijo el supuesto de partida
sobre la estructura documental y cambió el exit criterion del discovery. → `D-0032`

Los 28 huérfanos en scope recibieron disposición humana explícita, entidad por entidad,
por fuera de cualquier inferencia automática.

## Qué NO se verificó

- **La completitud del export.** Los exports operativos del 16/09 y los catálogos del
  18/09 no tienen garantía transaccional común; la completitud no está certificada.
- **Que las 15 carpetas inaccesibles no existan.** Un 404 bajo la identidad utilizada
  no prueba inexistencia, y la muestra no acredita acceso para otra identidad.
- **La semántica técnica de lookups, `Created_Time` y zona horaria.** Queda pendiente y
  precede al importador.
- **La ejecución agéntica.** Sin AgentRun registrado, no hay vínculo run–diff.
- **Nada sobre la migración.** El cierre del discovery no la autoriza ni la valida.
